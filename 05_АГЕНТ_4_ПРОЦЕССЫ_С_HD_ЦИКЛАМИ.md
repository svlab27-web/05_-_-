# Агент 4: ПРОЦЕССЫ И АВТОМАТИЗАЦИЯ

## 🎯 Назначение Агента

**Агент 4** — это операционный менеджер-оптимизатор. Он помогает:
- ✅ Найти где теряешь время (аудит времени)
- ✅ Выявить рутинные процессы, которые можно автоматизировать
- ✅ Масштабировать без найма людей (первый уровень оптимизации)
- ✅ Повысить маржу через экономию времени

**Запускается:** Параллельно с Агентами 1, 2, 3 (после Агента 0)
**Входные данные:** Профиль (что ты делаешь, сколько времени, какие боли) из Агента 0 Блок 4
**Выходные данные:** Аудит процессов, список автоматизаций, экономия времени в часах

---

## 🔁 HD ЦИКЛЫ (Автоматические фоновые процессы)

HD циклы — это повторяющиеся автоматические задачи, которые агент выполняет без участия человека. Они мониторят процессы, обновляют статусы и предупреждают о проблемах.

### Цикл 1: МОНИТОРИНГ ЗАЯВОК
**Периодичность:** Каждые 10 минут  
**Назначение:** Проверка новых и "застрявших" заявок

```
Что делает цикл:

1. Проверка новых заявок:
   ├─ Сканирует источники (Email, Telegram, Google Forms)
   ├─ Выявляет необработанные заявки (новые за последние 10 минут)
   ├─ Помечает их статусом "Новая"
   └─ Отправляет уведомление оператору

2. Проверка "застрявших" заявок:
   ├─ Ищет заявки со статусом "В работе" > 2 часов без изменений
   ├─ Помечает их как "Требует внимания"
   ├─ Эскалирует руководителю (алерт в Telegram)
   └─ Логирует в базу данных

3. Критические ситуации:
   ├─ Если более 5 новых заявок за раз → алерт "Высокая нагрузка"
   ├─ Если заявка от VIP клиента → приоритет "Срочно"
   └─ Если заявка просрочена > 24 часа → эскалация менеджеру

Технически:
├─ Запрос к БД: SELECT * FROM requests WHERE status='new' OR (status='in_progress' AND updated_at < NOW() - INTERVAL 2 HOURS)
├─ API проверка: Email (IMAP), Telegram Bot API, Google Forms API
└─ Уведомления: Telegram Bot, Email, Push
```

---

### Цикл 2: ОБНОВЛЕНИЕ СТАТУСОВ
**Периодичность:** Каждые 30 минут  
**Назначение:** Синхронизация статусов заказов и проверка сроков

```
Что делает цикл:

1. Синхронизация статусов:
   ├─ Проверяет все заказы со статусом "В работе"
   ├─ Обновляет прогресс выполнения (%)
   ├─ Синхронизирует с внешними системами (если есть API)
   └─ Логирует изменения в историю

2. Проверка сроков:
   ├─ Сравнивает текущую дату с дедлайном
   ├─ Если осталось < 20% времени и прогресс < 50% → алерт "Риск срыва срока"
   ├─ Если просрочен → статус "Просрочен" + уведомление
   └─ Расчет среднего времени выполнения (для прогнозов)

3. Автоматические действия:
   ├─ Если заказ выполнен на 100% → автоматически перевести в "На проверку"
   ├─ Если заказ просрочен > 3 дней → эскалация руководителю
   └─ Если заказ завершен → отправить клиенту уведомление

Технически:
├─ Запрос к БД: SELECT * FROM orders WHERE status IN ('in_progress', 'on_review')
├─ Логика: Если (deadline - current_date) / (deadline - start_date) < 0.2 AND progress < 50% → ALERT
├─ Обновление: UPDATE orders SET status='on_review' WHERE progress=100
└─ Уведомления: Telegram, Email
```

---

### Цикл 3: КОНТРОЛЬ КАЧЕСТВА
**Периодичность:** Каждые 2 часа  
**Назначение:** Проверка завершенных заказов на соответствие стандартам

```
Что делает цикл:

1. Проверка завершенных заказов:
   ├─ Выбирает заказы со статусом "На проверку"
   ├─ Проверяет наличие всех обязательных документов
   ├─ Сверяет с чек-листом качества (если настроен)
   └─ Помечает "Готов к отправке" или "Требует доработки"

2. Выявление проблем качества:
   ├─ Анализирует историю правок (если > 3 правок → отметка "Проблема качества")
   ├─ Проверяет отзывы клиентов (если есть негативные → флаг "Внимание")
   ├─ Сравнивает с SLA (время выполнения, качество)
   └─ Генерирует отчет по проблемным заказам

3. Инициирование повторной проверки:
   ├─ Если заказ на проверке > 4 часов → напомнить контролеру
   ├─ Если найдены критические ошибки → вернуть исполнителю
   └─ Если все ОК → автоматически отправить клиенту (если настроено)

Технически:
├─ Запрос к БД: SELECT * FROM orders WHERE status='on_review'
├─ Логика: Проверка наличия файлов, соответствие чек-листу
├─ Анализ: История изменений, количество правок
└─ Автоматизация: Отправка клиенту через API (Email, Telegram)
```

---

### Цикл 4: ОТЧЕТНОСТЬ И АНАЛИТИКА
**Периодичность:** Раз в сутки (в 09:00 утра)  
**Назначение:** Формирование сводок и дашбордов с метриками

```
Что делает цикл:

1. Генерация ежедневного отчета:
   ├─ Количество новых заявок за сутки
   ├─ Количество завершенных заказов
   ├─ Среднее время обработки заявки
   ├─ Среднее время выполнения заказа
   ├─ Количество просроченных задач
   └─ Процент успешности (без доработок)

2. Анализ узких мест:
   ├─ Где больше всего "застревает" заявок (этап процесса)
   ├─ Кто из исполнителей работает медленнее нормы
   ├─ Какие типы заказов чаще требуют доработки
   └─ В какое время суток пиковая нагрузка

3. Формирование дашборда:
   ├─ Визуализация метрик (графики, диаграммы)
   ├─ Тренды за неделю/месяц
   ├─ Прогноз нагрузки на следующий день
   └─ Рекомендации по оптимизации

4. Отправка отчетов:
   ├─ Руководителю → полный отчет (PDF + ссылка на дашборд)
   ├─ Исполнителям → их личные метрики
   └─ Сохранение в архив для истории

Технически:
├─ Запросы к БД: Агрегация данных за 24 часа
├─ Расчеты: AVG(processing_time), COUNT(*), SUM(*)
├─ Визуализация: Генерация графиков (через Plotly, Chart.js)
├─ Экспорт: PDF через ReportLab или WeasyPrint
└─ Отправка: Email через Mailchimp API, Telegram Bot
```

---

## 📊 Таблица HD циклов

| Цикл | Периодичность | Назначение | Критические алерты |
|------|---------------|------------|-------------------|
| **Мониторинг заявок** | Каждые 10 мин | Проверка новых и застрявших заявок | > 5 новых заявок, VIP клиенты, просрочка > 24ч |
| **Обновление статусов** | Каждые 30 мин | Синхронизация прогресса, проверка сроков | Риск срыва срока, просрочка > 3 дней |
| **Контроль качества** | Каждые 2 часа | Проверка завершенных заказов | > 3 правок, негативные отзывы |
| **Отчетность** | Раз в сутки (09:00) | Формирование сводок и дашбордов | Критическое падение метрик |

---

## 🛠️ Технические детали циклов

```python
# Пример структуры HD цикла в коде

import schedule
import time
from datetime import datetime, timedelta

# Цикл 1: Мониторинг заявок (каждые 10 минут)
def monitor_requests():
    # Проверка новых заявок
    new_requests = db.query("SELECT * FROM requests WHERE status='new' AND created_at > NOW() - INTERVAL 10 MINUTES")
    if len(new_requests) > 0:
        send_notification(f"Новых заявок: {len(new_requests)}")
    
    # Проверка застрявших заявок
    stuck_requests = db.query("SELECT * FROM requests WHERE status='in_progress' AND updated_at < NOW() - INTERVAL 2 HOURS")
    if len(stuck_requests) > 0:
        send_alert(f"Застрявших заявок: {len(stuck_requests)}")

schedule.every(10).minutes.do(monitor_requests)

# Цикл 2: Обновление статусов (каждые 30 минут)
def update_statuses():
    orders = db.query("SELECT * FROM orders WHERE status IN ('in_progress', 'on_review')")
    for order in orders:
        # Проверка сроков
        if order.deadline < datetime.now() and order.status != 'completed':
            order.status = 'overdue'
            send_alert(f"Заказ {order.id} просрочен!")
        
        # Автоматический переход статуса
        if order.progress >= 100:
            order.status = 'on_review'
    
    db.commit()

schedule.every(30).minutes.do(update_statuses)

# Цикл 3: Контроль качества (каждые 2 часа)
def quality_control():
    orders_to_check = db.query("SELECT * FROM orders WHERE status='on_review'")
    for order in orders_to_check:
        # Проверка чек-листа
        if check_quality_checklist(order):
            order.status = 'ready_to_send'
            send_to_client(order)
        else:
            order.status = 'needs_rework'
            notify_executor(order)

schedule.every(2).hours.do(quality_control)

# Цикл 4: Отчетность (раз в сутки в 09:00)
def daily_report():
    report = generate_report()
    send_email(report, recipients=['manager@company.com'])
    save_to_archive(report)

schedule.every().day.at("09:00").do(daily_report)

# Запуск всех циклов
while True:
    schedule.run_pending()
    time.sleep(60)  # Проверка каждую минуту
```

---

_(Остальные разделы документа - Процесс работы Агента 4, Интеграция, Технические требования, Метрики успеха - остаются без изменений из оригинального файла)_

---

## 🔄 Интеграция с другими агентами

**Агент 4 ← Агент 0:**
- Получает данные о времени (Блок 4 из анкеты)
- Получает боли (Блок 4: "Что отнимает время?")

**Агент 4 → Агент 5 (Итоговый план):**
- Передает список экономии времени
- Передает рекомендации по инструментам
- Включается в финальный 30/60/90 день план

---

## 🛠️ Технические требования

- **Язык:** Python + API интеграции
- **БД:** PostgreSQL для хранения аудита
- **Интеграции:** Calendly API, Stripe API, Mailchimp API
- **Анализ:** временные логи, распределение часов
- **Скорость:** 2-3 минуты на анализ
- **Точность:** 85%+ (зависит от честности ввода данных)
- **HD Циклы:** schedule library для автоматизации фоновых процессов

---

## 📊 Метрики успеха

✅ **Агент 4 успешен, если:**
- Выявлены 5-10 автоматизаций
- Эксперт понимает потенциальную экономию часов
- Видны конкретные инструменты (не просто "автоматизировать")
- Рассчитан ROI для каждого инструмента
- Есть пошаговый план внедрения (что внедрять в первую очередь)
- Внедрено минимум 2 инструмента в первый месяц
- **HD циклы работают корректно и мониторят процессы 24/7**

❌ **Что не работает:**
- Автоматизации не релевантны (TikTok для B2B консультанта)
- Нет расчета экономии времени
- Инструменты дорогие или неудачные
- План внедрения нереалистичный (все сразу)
- Экономия не материализуется на практике (неправильное внедрение)
- **HD циклы не запускаются или генерируют избыточные алерты**
